<?xml version="1.0" encoding="UTF-8"?>
<!-- Strict Unit-only config. requireCoverageMetadata forces every unit test to declare what it covers,
     so mutation MSI reflects targeted coverage rather than incidental collaborator execution. The
     Integration suite runs through phpunit.dist.xml, which does not require coverage metadata because
     integration tests boot WordPress and legitimately carry none. -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
		 bootstrap="tests/bootstrap.php"
		 colors="true"
		 cacheDirectory="tests/.cache/phpunit"
		 executionOrder="depends,defects"
		 requireCoverageMetadata="true"
		 beStrictAboutCoverageMetadata="true"
		 beStrictAboutOutputDuringTests="true"
		 failOnRisky="true"
		 failOnWarning="true"
		 defaultTestSuite="Unit">
	<testsuites>
		<testsuite name="Unit">
			<directory>packages/*/tests/Unit</directory>
		</testsuite>
	</testsuites>

	<source>
		<include>
			<directory>packages/*/src</directory>
		</include>
	</source>
</phpunit>
