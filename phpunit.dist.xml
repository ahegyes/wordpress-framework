<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
		 bootstrap="tests/bootstrap.php"
		 colors="true"
		 cacheDirectory="tests/.cache/phpunit"
		 executionOrder="depends,defects"
		 requireCoverageMetadata="false"
		 beStrictAboutCoverageMetadata="true"
		 beStrictAboutOutputDuringTests="true"
		 failOnRisky="true"
		 failOnWarning="true"
		 defaultTestSuite="Unit">
	<!-- requireCoverageMetadata is off here and enforced only on phpunit.unit.xml: the Unit suite declares
	     coverage so mutation MSI stays honest, while the Integration suite boots WordPress and carries no
	     coverage metadata, so requiring it would mark every integration test risky. -->
	<testsuites>
		<testsuite name="Unit">
			<directory>packages/*/tests/Unit</directory>
		</testsuite>
		<testsuite name="Integration">
			<directory>packages/*/tests/Integration</directory>
		</testsuite>
	</testsuites>

	<php>
		<!-- The integration suite boots WordPress + WooCommerce per process; raise the limit above the
		     container default so the full run, and the shutdown error handlers, stay within budget. -->
		<ini name="memory_limit" value="512M"/>
	</php>

	<source>
		<include>
			<directory>packages/*/src</directory>
		</include>
	</source>
</phpunit>
